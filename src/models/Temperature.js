export default class Temperature {

    min: Number;
    max: Number;
    avg: Number;

    constructor(min: Number, max: Number, avg: Number) {
        this.min = min;
        this.max = max;
        this.avg = avg;
    }
}